# Examples

Runnable examples for this crate. Add a file such as `examples/demo.rs` and run
it with `cargo run --example demo`.

Typical uses:

- Demonstrate the crate's public API in a small, self-contained program.
- Show a common workflow or integration end to end.
- Provide a quick, runnable reference for a specific feature.
